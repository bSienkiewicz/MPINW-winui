<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App1.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ImageTemplate" x:DataType="local:ExpandItem">
            <ItemContainer HorizontalAlignment="Left" Width="200" Height="140" AutomationProperties.Name="{x:Bind Title}">
                <TextBlock Text="{x:Bind Title}" VerticalAlignment="Top" Margin="10,0,0,0"/>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <StackPanel>
        <Button Content="Standard XAML button" Click="Button_Click" />
        <ItemsView Width="220" Height="400" HorizontalAlignment="Left" ItemsSource="{x:Bind Items}"
            ItemTemplate="{StaticResource ImageTemplate}"/>
    </StackPanel>
</Page>
