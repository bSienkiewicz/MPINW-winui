<Page
    x:Class="App1.Alerting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:App1.Helpers"
    xmlns:local="using:App1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar
            Name="infoBar"
            Grid.Row="0"
            IsOpen="False"
            Severity="Error" />

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Alerting" />

            <StackPanel
                Grid.Column="2"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="10">
                <ComboBox
                    x:Name="stacksComboBox"
                    Width="200"
                    PlaceholderText="Select a stack..."
                    SelectionChanged="StackComboBox_SelectionChanged" />
                <Button x:Name="selectFolderButton" Click="SelectFolderButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE838;" />
                        <TextBlock Text="Select Repository Folder" />
                    </StackPanel>
                </Button>
                <Button x:Name="openFolderButton" Click="OpenFolderButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8DA;" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!--  Left Column with ScrollView  -->
            <Grid
                Name="EditPanel"
                Grid.Column="0"
                Padding="10"
                Background="{ThemeResource LayerFillColorDefaultBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid
                    Grid.Row="0"
                    Margin="0,0,0,15"
                    ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Name="NRAlertSearch"
                        Grid.Column="0"
                        PlaceholderText="Search for an alert"
                        TextChanged="NRAlertSearch_TextChanged" />
                    <Button
                        Grid.Column="1"
                        Click="AddNewAlertButton_Click"
                        Style="{StaticResource AccentButtonStyle}">
                        <FontIcon Glyph="&#xE710;" />
                    </Button>
                    <Button Grid.Column="2" Click="GenerateAlertButton_Click">
                        <FontIcon Glyph="&#xE9F5;" />
                    </Button>
                </Grid>

                <ScrollView
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                    <ListView
                        x:Name="AlertsListView"
                        ItemsSource="{x:Bind AlertItems, Mode=OneWay}"
                        SelectionChanged="AlertsListView_SelectionChanged"
                        SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="helpers:NrqlAlert">
                                <StackPanel Margin="10">
                                    <TextBlock Text="{x:Bind Name}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollView>
            </Grid>

            <!--  Right Column  -->
            <Grid
                Grid.Column="1"
                Padding="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                RowSpacing="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <ScrollView Grid.Row="0">
                    <StackPanel Padding="10" Spacing="10">
                        <CheckBox Content="Enabled" IsChecked="{x:Bind SelectedAlert.Enabled, Mode=TwoWay}" />
                        <TextBox
                            x:Name="NameTextBox"
                            Header="Name:"
                            Text="{x:Bind SelectedAlert.Name, Mode=TwoWay}" />
                        <TextBox Header="Severity" Text="{x:Bind SelectedAlert.Severity, Mode=TwoWay}" />
                        <TextBox
                            AcceptsReturn="True"
                            Header="Description:"
                            Text="{x:Bind SelectedAlert.Description, Mode=TwoWay}"
                            TextWrapping="Wrap" />
                        <TextBox
                            MinHeight="100"
                            AcceptsReturn="True"
                            Header="NRQL Query:"
                            Text="{x:Bind SelectedAlert.NrqlQuery, Mode=TwoWay}"
                            TextWrapping="Wrap" />
                        <TextBox Header="Runbook URL" Text="{x:Bind SelectedAlert.RunbookUrl, Mode=TwoWay}" />
                        <TextBox Header="AggregationMethod" Text="{x:Bind SelectedAlert.AggregationMethod, Mode=TwoWay}" />
                        <TextBox Header="AggregationDelay" Text="{x:Bind SelectedAlert.AggregationDelay, Mode=TwoWay}" />
                        <TextBox Header="CriticalOperator" Text="{x:Bind SelectedAlert.CriticalOperator, Mode=TwoWay}" />
                        <TextBox Header="CriticalThreshold" Text="{x:Bind SelectedAlert.CriticalThreshold, Mode=TwoWay}" />
                        <TextBox Header="CriticalThresholdDuration" Text="{x:Bind SelectedAlert.CriticalThresholdDuration, Mode=TwoWay}" />
                        <TextBox Header="CriticalThresholdOccurrences" Text="{x:Bind SelectedAlert.CriticalThresholdOccurrences, Mode=TwoWay}" />
                    </StackPanel>
                </ScrollView>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <!--  Bottom action button row LEFT  -->
                    <StackPanel
                        Grid.Column="0"
                        Orientation="Horizontal"
                        Spacing="10">
                        <Button x:Name="cloneButton">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xF584;" />
                                <TextBlock Text="Duplicate" />
                            </StackPanel>
                            <Button.Flyout>
                                <Flyout>
                                    <StackPanel>
                                        <TextBlock
                                            Margin="0,0,0,12"
                                            Style="{ThemeResource BaseTextBlockStyle}"
                                            Text="This will create a copy of this alert." />
                                        <Button Click="CopyAlertButton_Click" Content="Create a copy" />
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                        <Button
                            x:Name="deleteButton"
                            Background="IndianRed"
                            Foreground="Black">
                            <FontIcon Glyph="&#xE74D;" />
                            <Button.Flyout>
                                <Flyout>
                                    <StackPanel>
                                        <TextBlock
                                            Margin="0,0,0,12"
                                            Style="{ThemeResource BaseTextBlockStyle}"
                                            Text="The alert will be removed, continue?" />
                                        <Button
                                            Background="IndianRed"
                                            Click="DeleteAlertButton_Click"
                                            Content="Yes, delete the alert" />
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                    <!--  Bottom action button row RIGHT  -->
                    <StackPanel
                        Grid.Column="2"
                        Orientation="Horizontal"
                        Spacing="10">
                        <Button
                            x:Name="saveButton"
                            Click="SaveSelectedAlertButton_Click"
                            Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74E;" />
                                <TextBlock Text="Save" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="cancelButton">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE711;" />
                                <TextBlock Text="Cancel" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>

    </Grid>
</Page>