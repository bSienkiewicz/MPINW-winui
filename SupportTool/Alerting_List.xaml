<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SupportTool.Alerting_List"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SupportTool"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SupportTool.Models"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="10">
        <Grid.RowDefinitions>
            <!--  InfoBar Row  -->
            <RowDefinition Height="Auto" />
            <!--  Header Row  -->
            <RowDefinition Height="Auto" />
            <!--  Content Row  -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  InfoBar for API Key Warning  -->
        <InfoBar
            x:Name="InfoBar"
            Title="API Key Missing"
            Grid.Row="0"
            Grid.ColumnSpan="3"
            Margin="0,0,0,20"
            IsOpen="False"
            Message="The API key is not set. Please configure it in the settings."
            Severity="Warning">
            <InfoBar.ActionButton>
                <Button Click="ApiKeyWarningInfoBar_ButtonClick" Content="Go to Settings" />
            </InfoBar.ActionButton>
        </InfoBar>

        <!--  Header  -->
        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="0,0,0,20"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Alerting List" />

            <StackPanel
                Grid.Column="2"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="10">
                <ComboBox
                    x:Name="stacksComboBox"
                    Width="200"
                    PlaceholderText="Select a stack..."
                    SelectionChanged="StackComboBox_SelectionChanged" />
                <Button
                    x:Name="FetchNRButton"
                    Click="FetchNRButton_Click"
                    Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xEBD3;" />
                        <TextBlock Text="Fetch" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>



        <!--  AppNames Column  -->
        <Grid
            Grid.Row="2"
            Grid.Column="0"
            Padding="5"
            VerticalAlignment="Stretch"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            CornerRadius="6"
            RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--<SelectorBar x:Name="SelectorBar1">
                <SelectorBarItem
                    x:Name="SelectorBarItemRecent"
                    Icon="List"
                    Text="All PD/ER" />
                <SelectorBarItem
                    x:Name="SelectorBarItemShared"
                    Icon="Important"
                    IsSelected="True"
                    Text="Missing" />
                <SelectorBarItem
                    x:Name="SelectorBarItemFavorites"
                    Icon="Accept"
                    Text="Existing" />
            </SelectorBar>-->

            <Grid Grid.Row="1" Margin="10,0,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" MinWidth="100" />
                    <ColumnDefinition Width="1*" MinWidth="100" />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    Content="Client Name"
                    CornerRadius="5,0,0,5" />
                <Button
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Content="Carrier Name"
                    CornerRadius="0,0,0,0" />
                <Button
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    Content="PD Alert"
                    CornerRadius="0,0,0,0" />
                <Button
                    Grid.Column="3"
                    HorizontalAlignment="Stretch"
                    Content="ER Alert"
                    CornerRadius="0,5,5,0" />
            </Grid>

            <ProgressRing
                x:Name="AppNameFetchingProgress"
                Grid.Row="2"
                IsActive="True"
                Visibility="Collapsed" />

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ListView x:Name="AppNamesList" DoubleTapped="AppNamesList_DoubleTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:AppCarrierItem">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="1*" MinWidth="100" />
                                    <ColumnDefinition Width="1*" MinWidth="100" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    Text="{Binding AppName}" />
                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Text="{Binding CarrierName}" />

                                <CheckBox
                                    x:Name="PDCheck"
                                    Grid.Column="2"
                                    Content="Print Duration Alert"
                                    IsChecked="{x:Bind HasPrintDurationAlert}"
                                    IsEnabled="False" />

                                <CheckBox
                                    x:Name="ERCheck"
                                    Grid.Column="3"
                                    Content="Error Rate Alert"
                                    IsChecked="{x:Bind HasErrorRateAlert}"
                                    IsEnabled="False" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>

            <TeachingTip
                x:Name="AlertingListToast"
                Title="Change themes without hassle"
                CloseButtonContent="Got it!"
                IsLightDismissEnabled="True"
                PlacementMargin="20"
                PreferredPlacement="Auto"
                Subtitle="It's easier than ever to see control samples in both light and dark theme!" />
        </Grid>

    </Grid>
</Page>